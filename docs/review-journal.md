# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its mobile workflows focus without claiming live deployment or external usage.

## Cases

- `baseline`: `form pressure`, score 212, lane `ship`
- `stress`: `sync drift`, score 178, lane `ship`
- `edge`: `local state`, score 216, lane `ship`
- `recovery`: `conflict cost`, score 211, lane `ship`
- `stale`: `form pressure`, score 191, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
